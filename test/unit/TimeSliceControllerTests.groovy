
import org.springframework.test.AssertThrows
/**
 * 
 */

import junit.framework.TestCase


/**
 * @author henning
 *
 */
public class TimeSliceControllerTests extends TestBase{

    /**
     * Prerequisites: TimeSlice DAO must return a slice to be edited
     * Expectation: when called with the edit-candiate's id, it's data must be place in the context
     */
    void testEdit(){
        // TODO: add proper test here, so we do not rely only on webtest!
    }
   
    
}
